import Link from "next/link";

export default async function InformationDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const title = decodeURIComponent(slug).replaceAll("-", " "); return <main className="min-h-screen bg-slate-950 px-4 py-16 text-slate-100 lg:px-8"><article className="mx-auto max-w-4xl"><Link href="/discover" className="text-sm text-emerald-400">← Məlumatlar</Link><p className="mt-12 text-sm uppercase tracking-[0.35em] text-emerald-400">Enerji bilikləri</p><h1 className="mt-4 text-4xl font-semibold capitalize sm:text-6xl">{title}</h1><p className="mt-8 max-w-2xl text-lg leading-9 text-slate-300">Bu mövzu üzrə Texnoland mütəxəssislərinin hazırladığı praktik məlumatlar, texnologiyalar və tətbiq nümunələri.</p></article></main>; }