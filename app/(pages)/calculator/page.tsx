"use client";
import { useState } from "react";

export default function CalculatorPage() { const [monthly, setMonthly] = useState(600); const [sunHours, setSunHours] = useState(4.5); const system = Math.max(1, monthly / 30 / sunHours / 0.8); const panels = Math.ceil(system / 0.55); const annual = Math.round(system * sunHours * 365 * 0.8); return <main className="min-h-screen bg-slate-950 px-4 py-16 text-slate-100 lg:px-8"><div className="mx-auto grid max-w-5xl gap-8 lg:grid-cols-[0.8fr_1.2fr]"><div><p className="text-sm uppercase tracking-[0.35em] text-emerald-400">Faydalı məlumat</p><h1 className="mt-3 text-4xl font-semibold sm:text-5xl">Günəş enerjisi potensialınızı hesablayın</h1><p className="mt-5 leading-8 text-slate-400">Təxmini hesablamadır. Dəqiq layihə üçün mühəndis qiymətləndirməsi tələb olunur.</p></div><div className="border border-white/10 bg-white/[0.04] p-6 sm:p-8"><label className="block text-sm text-slate-300">Aylıq sərfiyyat: <strong className="text-emerald-300">{monthly} kVt·st</strong><input type="range" min="100" max="5000" step="50" value={monthly} onChange={(event) => setMonthly(Number(event.target.value))} className="mt-4 w-full accent-emerald-500" /></label><label className="mt-8 block text-sm text-slate-300">Peak sun hours: <strong className="text-emerald-300">{sunHours}</strong><input type="range" min="2" max="7" step="0.1" value={sunHours} onChange={(event) => setSunHours(Number(event.target.value))} className="mt-4 w-full accent-emerald-500" /></label><div className="mt-10 grid gap-3 sm:grid-cols-3"><div className="border border-white/10 p-4"><p className="text-xs text-slate-400">Sistem gücü</p><p className="mt-2 text-2xl font-semibold">{system.toFixed(1)} kVt</p></div><div className="border border-white/10 p-4"><p className="text-xs text-slate-400">Panel sayı</p><p className="mt-2 text-2xl font-semibold">{panels}</p></div><div className="border border-white/10 p-4"><p className="text-xs text-slate-400">İllik istehsal</p><p className="mt-2 text-2xl font-semibold">{annual} kVt·st</p></div></div></div></div></main>; }
