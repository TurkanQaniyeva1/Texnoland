import Link from "next/link";
import { notFound } from "next/navigation";
import Image from "next/image";
import { companyProjects } from "@/lib/companyData";

export function generateStaticParams() { return companyProjects.map((project) => ({ slug: project.slug })); }

export default async function ProjectDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = companyProjects.find((item) => item.slug === slug); if (!project) notFound(); const related = companyProjects.filter((item) => item.slug !== slug).slice(0, 3); return <main className="min-h-screen bg-slate-950 px-4 py-16 text-slate-100 lg:px-8"><article className="mx-auto max-w-5xl"><Link href="/projects" className="text-sm text-emerald-400">← Bütün layihələr</Link><div className="mt-12 grid gap-10 lg:grid-cols-[1.2fr_0.8fr]"><div className="relative min-h-80 overflow-hidden border border-emerald-400/20"><Image src="/images/min_mekteb.jpg" alt={project.title} fill sizes="(max-width: 1024px) 100vw, 60vw" className="object-cover opacity-55" /><div className="absolute inset-0 bg-gradient-to-t from-slate-950 via-slate-950/20 to-transparent" /><div className="absolute bottom-8 left-8 right-8"><p className="text-sm uppercase tracking-[0.3em] text-emerald-300">{project.type}</p><h1 className="mt-6 text-4xl font-semibold sm:text-6xl">{project.title}</h1></div></div><div className="border border-white/10 bg-white/[0.04] p-7"><p className="text-sm text-slate-400">Məkan</p><p className="mt-1 text-lg font-semibold">{project.location}</p><p className="mt-7 text-sm text-slate-400">Güc / tutum</p><p className="mt-1 text-lg font-semibold text-emerald-300">{project.capacity ?? "Layihəyə uyğun"}</p><p className="mt-7 text-sm text-slate-400">Texnologiya</p><p className="mt-1 text-lg font-semibold">Enerji monitorinqi və təhlükəsiz quraşdırma</p></div></div><div className="mt-12 grid gap-8 lg:grid-cols-[1fr_.8fr]"><div><h2 className="text-2xl font-semibold">Layihə haqqında</h2><p className="mt-4 text-lg leading-9 text-slate-300">{project.description} Texnoland komandası layihənin təhlükəsizlik, səmərəlilik və uzunmüddətli istismar tələblərini nəzərə alaraq texniki həlli həyata keçirib.</p></div><div className="border-l border-emerald-400/40 pl-6"><p className="text-sm uppercase tracking-[0.2em] text-emerald-300">Nəticə</p><p className="mt-3 leading-8 text-slate-400">Daha dayanıqlı enerji təminatı, optimallaşdırılmış sərfiyyat və etibarlı istismar.</p></div></div><div className="mt-12"><h2 className="text-2xl font-semibold">Əlaqəli layihələr</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{related.map((item) => <Link key={item.slug} href={`/projects/${item.slug}`} className="border border-white/10 p-4 hover:border-emerald-400/60"><p className="font-semibold">{item.title}</p><p className="mt-2 text-sm text-slate-400">{item.location}</p></Link>)}</div></div><div className="mt-10"><Link href="/quote" className="rounded-full bg-emerald-600 px-5 py-3 text-sm font-semibold">Oxşar layihə üçün təklif alın</Link></div></article></main>; }
