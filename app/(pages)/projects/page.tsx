import Link from "next/link";
import { companyProjects } from "@/lib/companyData";

export default function ProjectsPage() { return <main className="min-h-screen bg-slate-950 px-4 py-16 text-slate-100 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-sm uppercase tracking-[0.35em] text-emerald-400">Portfolio</p><h1 className="mt-3 text-4xl font-semibold sm:text-5xl">Gördüyümüz işlər</h1><p className="mt-5 max-w-2xl text-slate-400">Fərqli miqyaslı enerji və mühəndislik layihələrindən seçilmiş nümunələr.</p><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{companyProjects.map((project) => <Link key={project.slug} href={`/projects/${project.slug}`} className="flex min-h-64 flex-col justify-between border border-white/10 bg-gradient-to-br from-emerald-400/10 to-white/[0.03] p-5 transition hover:-translate-y-1 hover:border-emerald-400/60"><div><p className="text-xs uppercase tracking-[0.2em] text-emerald-300">{project.type}</p><h2 className="mt-5 text-xl font-semibold">{project.title}</h2></div><div className="text-sm text-slate-400"><p>{project.location}</p>{project.capacity ? <p className="mt-1 text-emerald-300">{project.capacity}</p> : null}</div></Link>)}</div></div></main>; }
